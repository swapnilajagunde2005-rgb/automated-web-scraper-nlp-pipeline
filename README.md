# automated-web-scraper-nlp-pipeline
A full-stack data product that scrapes unstructured web data dynamically and passes it through an NLTK VADER NLP engine to render live sentiment analytics using Streamlit.
